# Devnet RPC (optional)
DEVNET_RPC=https://api.devnet.solana.com

# Pinata auth (required for uploading local JSONs)
# Generate a JWT in Pinata and paste it here (never commit your real .env)
PINATA_JWT=YOUR_PINATA_JWT_HERE

# Optional: customize gateway base (defaults to https://gateway.pinata.cloud/ipfs)
PINATA_GATEWAY_BASE=https://gateway.pinata.cloud/ipfs

